# Backend API Configuration
# api.wealthwarriorshub.in
# HTTP - redirect to HTTPS
 server {
      listen 80;
      listen [::]:80;
      server_name api.wealthwarriorshub.in;
      return 301 https://$server_name$request_uri;
 }
server {
    server_name api.wealthwarriorshub.in;


    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # Logging
    access_log /var/log/nginx/api.wealthwarriorshub.in.access.log;
    error_log /var/log/nginx/api.wealthwarriorshub.in.error.log;

    # API endpoint
    location / {
        proxy_pass http://localhost:8000;
        proxy_http_version 1.1;

        # Proxy headers
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;

        # WebSocket support (if needed)
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # Health check endpoint
    location /health {
        proxy_pass http://localhost:8000/health;
        access_log off;
    }

    # Block common exploits
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }

    listen [::]:443 ssl ipv6only=on; # managed by Certbot
    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/api.wealthwarriorshub.in/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/api.wealthwarriorshub.in/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot

}


server {
    if ($host = api.wealthwarriorshub.in) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


    listen 80;
    listen [::]:80;

    server_name api.wealthwarriorshub.in;
    return 404; # managed by Certbot


}
root@ubuntu-s-4vcpu-8gb-blr1-01:/etc/nginx/sites-enabled# sudo nano api.wealthwarriorshub.in
root@ubuntu-s-4vcpu-8gb-blr1-01:/etc/nginx/sites-enabled# sudo nano api.wealthwarriorshub.in
root@ubuntu-s-4vcpu-8gb-blr1-01:/etc/nginx/sites-enabled# cat nano api.wealthwarriorshub.in
cat: nano: No such file or directory

# Backend API Configuration
# api.wealthwarriorshub.in
# HTTP - redirect to HTTPS
 server {
      listen 80;
      listen [::]:80;
      server_name api.wealthwarriorshub.in;
      return 301 https://$server_name$request_uri;
 }
server {
    server_name api.wealthwarriorshub.in;


    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # Logging
    access_log /var/log/nginx/api.wealthwarriorshub.in.access.log;
    error_log /var/log/nginx/api.wealthwarriorshub.in.error.log;

    # API endpoint
    location / {
        proxy_pass http://localhost:8000;
        proxy_http_version 1.1;

        # Proxy headers
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;

        # WebSocket support (if needed)
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
    # WebSocket configuration - CRITICAL FOR wss://
    location /ws/ {
        proxy_pass http://localhost:8000;

        # WebSocket upgrade headers - REQUIRED
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        # Standard proxy headers
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket timeouts
        proxy_read_timeout 86400s;
        proxy_send_timeout 86400s;
        proxy_connect_timeout 60s;

        # Disable buffering for WebSocket
        proxy_buffering off;

        # Allow longer query strings (for JWT tokens)
        large_client_header_buffers 4 32k;
    }
    # Health check endpoint
    location /health {
        proxy_pass http://localhost:8000/health;
        access_log off;
    }

    # Block common exploits
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }

    listen [::]:443 ssl ipv6only=on; # managed by Certbot
    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/api.wealthwarriorshub.in/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/api.wealthwarriorshub.in/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot

}


server {
    if ($host = api.wealthwarriorshub.in) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


    listen 80;
    listen [::]:80;

    server_name api.wealthwarriorshub.in;
    return 404; # managed by Certbot


}